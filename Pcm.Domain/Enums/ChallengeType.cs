namespace Pcm.Domain.Enums
{
    public enum ChallengeType
    {
        Friendly = 0,     // No stakes, just for fun
        Wager = 1         // Entry fee from both players, winner takes all
    }
}
